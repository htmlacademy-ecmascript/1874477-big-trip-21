const PointType = ['Taxi','Bus','Train','Ship','Drive','Flight','Check-in','Sightseeing','Restaurant'];
const FilterType = ['everything', 'future', 'present', 'past'];
const SortType = ['sort-price','sort-time','sort-day'];
const EmptyListText = [ 'Click New Event to create your first point', 'There are no past events now',
	'There are no present events now','There are no future events now'];

export { PointType, FilterType, EmptyListText, SortType };
